import { RemovePeriodsPipe } from './remove-periods.pipe';

describe('RemovePeriodsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemovePeriodsPipe();
    expect(pipe).toBeTruthy();
  });
});
